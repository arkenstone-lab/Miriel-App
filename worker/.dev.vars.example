# Worker local development secrets
# Copy this file to .dev.vars and fill in the values.
# This file is gitignored and should NEVER be committed.

# Required — generate with: openssl rand -hex 32
JWT_SECRET=

# Required for AI features — https://platform.openai.com/api-keys
# Without this, AI endpoints return mock/fallback responses
OPENAI_API_KEY=

# Optional — https://resend.com/api-keys
# Without this, email verification and password reset won't work
RESEND_API_KEY=

# Optional — comma-separated invite codes for signup restriction
# Leave empty to allow open registration
INVITE_CODES=
